<?php   

      /*
      $sql_view = "
			SELECT id, name, contrat, dateEmission, (prMin*(1+txRise)/numParc) as parcela,
			currency FROM Inform where (state=6 or state=7 or state=10) AND pgOk=0 AND codProd > 0 
			ORDER BY dateEmission";
      $cur=odbc_exec($db, $sql_view);
	
	  */
	
?>
